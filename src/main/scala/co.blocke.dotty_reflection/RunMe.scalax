package co.blocke.dotty_reflection

// import impl._ 
import info._

case class A[T]( a: T )

object RunMe extends App:


  Reflector.reflectOn[A[List[Option[Int]]]]

  println("done.")

  def methods(clazz: Class[_]): String = 
    s"=== Methods: ${clazz.getName} ===\n   " + clazz.getMethods.toList.mkString("\n   ")